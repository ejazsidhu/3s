import { FSUploaderDirective } from './fs-uploader.directive';

describe('FSUploaderDirective', () => {
  it('should create an instance', () => {
    const directive = new FSUploaderDirective();
    expect(directive).toBeTruthy();
  });
});
