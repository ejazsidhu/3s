import { HtmlSenitizationPipe } from './html-senitization.pipe';

describe('HtmlSenitizationPipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlSenitizationPipe();
    expect(pipe).toBeTruthy();
  });
});
