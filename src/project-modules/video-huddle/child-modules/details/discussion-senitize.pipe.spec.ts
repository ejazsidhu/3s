import { DiscussionSenitizePipe } from './discussion-senitize.pipe';

describe('DiscussionSenitizePipe', () => {
  it('create an instance', () => {
    const pipe = new DiscussionSenitizePipe();
    expect(pipe).toBeTruthy();
  });
});
