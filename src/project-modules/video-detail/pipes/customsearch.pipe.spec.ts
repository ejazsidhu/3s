import { CustomsearchPipe } from './customsearch.pipe';

describe('CustomsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
